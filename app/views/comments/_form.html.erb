<%= form_with model: [ @project, @project.comments.build] do |form|%>
<p>
	<%= form.text_area :content, row: 4, cols: 50, class: "bg-gray-200 w-fit p-4 "%>
</p>

<p >
	<%= form.submit  class:"text-white px-4 py-2 bg-blue-600 rounded-md hover:bg-blue-700 flex items-center gap-2 " do %><i class="fa-solid fa-circle-plus"></i> Add Comment
	<% end %>
</p>

<% end %>